# Clean URLs for the landing site. 200 (rewrite), not 301: the page is served
# at the pretty path rather than bouncing the browser to the .html one.
/support   /support.html   200
/privacy   /privacy.html   200

# og.html is the SOURCE for the social card, not a page anyone should land on.
/og        /               301
/og.html   /               301
