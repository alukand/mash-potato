# Secrets for Edge Functions. Copy to supabase/functions/.env for local dev
# (gitignored). For a hosted project: npx supabase secrets set TMDB_API_KEY=...
#
# TMDB v3 API key — https://www.themoviedb.org/settings/api
TMDB_API_KEY=
